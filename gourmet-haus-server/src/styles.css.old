/* ============================================
   GOURMET HAUS - ULTRA-LUXURY DESIGN SYSTEM
   A Revolutionary Dining Experience
   ============================================ */

/* ============================================
   CSS RESET & BASE CONFIGURATION
   ============================================ */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    /* Luxury Color Palette */
    --deep-black: #0a0a0a;
    --rich-black: #1a1a1a;
    --warm-black: #2a2520;
    --champagne-gold: #d4af37;
    --rose-gold: #b76e79;
    --platinum: #e5e4e2;
    --pearl-white: #f8f6f0;
    --burgundy: #800020;
    --forest-green: #2d5016;
    --midnight-blue: #191970;
    
    /* Glass Morphism */
    --glass-bg: rgba(255, 255, 255, 0.03);
    --glass-border: rgba(212, 175, 55, 0.15);
    --glass-shadow: rgba(0, 0, 0, 0.3);
    
    /* Typography */
    --font-display: 'Cinzel', 'Playfair Display', serif;
    --font-body: 'Cormorant Garamond', serif;
    --font-accent: 'Playfair Display', serif;
    
    /* Spacing System */
    --space-xs: 0.5rem;
    --space-sm: 1rem;
    --space-md: 1.5rem;
    --space-lg: 2.5rem;
    --space-xl: 4rem;
    --space-2xl: 6rem;
    
    /* Animation Curves */
    --ease-luxury: cubic-bezier(0.4, 0, 0.2, 1);
    --ease-smooth: cubic-bezier(0.16, 1, 0.3, 1);
    --ease-bounce: cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

/* ============================================
   GLOBAL STYLES
   ============================================ */
html {
    scroll-behavior: smooth;
    overflow-x: hidden;
}

body {
    font-family: var(--font-body);
    color: var(--pearl-white);
    background: var(--deep-black);
    line-height: 1.8;
    font-weight: 400;
    overflow-x: hidden;
    position: relative;
    letter-spacing: 0.02em;
}

/* ============================================
   PARTICLE CANVAS BACKGROUND
   ============================================ */
#particles-canvas {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    pointer-events: none;
    opacity: 0.3;
    will-change: auto;
}

/* ============================================
   ANIMATED GRADIENT ORBS
   ============================================ */
.gradient-orbs {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
    pointer-events: none;
    overflow: hidden;
}

.orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(100px);
    opacity: 0.12;
    animation: orbFloat 25s ease-in-out infinite;
    will-change: transform;
}

.orb-1 {
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, var(--champagne-gold), transparent);
    top: -200px;
    left: -200px;
    animation-delay: 0s;
}

.orb-2 {
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, var(--rose-gold), transparent);
    bottom: -150px;
    right: -150px;
    animation-delay: 7s;
}

.orb-3 {
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, var(--burgundy), transparent);
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    animation-delay: 14s;
}

@keyframes orbFloat {
    0%, 100% {
        transform: translate3d(0, 0, 0) scale(1);
    }
    33% {
        transform: translate3d(80px, -80px, 0) scale(1.08);
    }
    66% {
        transform: translate3d(-60px, 60px, 0) scale(0.92);
    }
}

/* ============================================
   LUXURY NAVIGATION
   ============================================ */
.luxury-nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem 4rem;
    background: rgba(10, 10, 10, 0.85);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--glass-border);
    transition: all 0.5s var(--ease-luxury);
}

.luxury-nav.scrolled {
    padding: 1rem 4rem;
    background: rgba(10, 10, 10, 0.95);
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
}

.nav-logo {
    font-family: var(--font-display);
    font-size: 2rem;
    font-weight: 900;
    color: var(--champagne-gold);
    letter-spacing: 0.2em;
    text-shadow: 0 0 30px rgba(212, 175, 55, 0.5);
}

.nav-logo-container {
    display: flex;
    align-items: center;
    height: 60px;
}

.nav-logo-svg {
    height: 50px;
    width: auto;
    filter: drop-shadow(0 0 20px rgba(212, 175, 55, 0.3));
    transition: all 0.4s var(--ease-luxury);
}

.nav-logo-svg:hover {
    filter: drop-shadow(0 0 30px rgba(212, 175, 55, 0.6));
    transform: scale(1.05);
}

.nav-links {
    display: flex;
    gap: 3rem;
}

.nav-link {
    font-family: var(--font-display);
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--platinum);
    text-decoration: none;
    letter-spacing: 0.15em;
    position: relative;
    transition: all 0.4s var(--ease-luxury);
    padding: 0.5rem 0;
}

.nav-link::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    width: 0;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--champagne-gold), transparent);
    transform: translateX(-50%);
    transition: width 0.4s var(--ease-luxury);
}

.nav-link:hover {
    color: var(--champagne-gold);
    text-shadow: 0 0 20px rgba(212, 175, 55, 0.5);
}

.nav-link:hover::after {
    width: 100%;
}

/* Mobile Menu Toggle */
.mobile-menu-toggle {
    display: none;
    flex-direction: column;
    gap: 5px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0.5rem;
    z-index: 1001;
}

.mobile-menu-toggle span {
    width: 25px;
    height: 2px;
    background: var(--champagne-gold);
    transition: all 0.3s ease;
    border-radius: 2px;
}

.mobile-menu-toggle.active span:nth-child(1) {
    transform: rotate(45deg) translate(7px, 7px);
}

.mobile-menu-toggle.active span:nth-child(2) {
    opacity: 0;
}

.mobile-menu-toggle.active span:nth-child(3) {
    transform: rotate(-45deg) translate(7px, -7px);
}

/* ============================================
   MAIN CONTAINER
   ============================================ */
.menu-container {
    position: relative;
    z-index: 10;
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
}

/* ============================================
   HERO SECTION
   ============================================ */
.hero-section {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
    margin-top: 80px;
    will-change: transform;
    backface-visibility: hidden;
    -webkit-backface-visibility: hidden;
    transform: translate3d(0, 0, 0);
    -webkit-transform: translate3d(0, 0, 0);
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
}

.hero-parallax-layers {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
}

.parallax-layer {
    position: absolute;
    width: 100%;
    height: 100%;
    background-size: cover;
    background-position: center;
}

.layer-back {
    background: radial-gradient(ellipse at 50% 50%, rgba(212, 175, 55, 0.05) 0%, transparent 60%);
    animation: layerFloat 30s ease-in-out infinite;
}

.layer-mid {
    background: radial-gradient(ellipse at 30% 70%, rgba(183, 110, 121, 0.05) 0%, transparent 50%);
    animation: layerFloat 25s ease-in-out infinite reverse;
}

.layer-front {
    background: radial-gradient(ellipse at 70% 30%, rgba(128, 0, 32, 0.05) 0%, transparent 50%);
    animation: layerFloat 20s ease-in-out infinite;
}

@keyframes layerFloat {
    0%, 100% {
        transform: translate3d(0, 0, 0) scale(1);
    }
    50% {
        transform: translate3d(20px, -20px, 0) scale(1.03);
    }
}

.brand-identity {
    text-align: center;
    position: relative;
    padding: 4rem 2rem;
    transform: translate3d(0, 0, 0);
    -webkit-transform: translate3d(0, 0, 0);
}

.brand-ornament {
    width: 150px;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--champagne-gold), transparent);
    margin: 0 auto;
    position: relative;
}

.brand-ornament.top {
    margin-bottom: 3rem;
    animation: ornamentGlow 3s ease-in-out infinite;
}

.brand-ornament.bottom {
    margin-top: 3rem;
    animation: ornamentGlow 3s ease-in-out infinite 1.5s;
}

@keyframes ornamentGlow {
    0%, 100% {
        opacity: 0.5;
        box-shadow: 0 0 10px var(--champagne-gold);
    }
    50% {
        opacity: 1;
        box-shadow: 0 0 30px var(--champagne-gold);
    }
}

.brand-ornament::before,
.brand-ornament::after {
    content: '◆';
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    color: var(--champagne-gold);
    font-size: 0.75rem;
    text-shadow: 0 0 20px var(--champagne-gold);
}

.brand-ornament::before { left: -20px; }
.brand-ornament::after { right: -20px; }

.brand-name {
    font-family: var(--font-display);
    font-size: clamp(3rem, 10vw, 7rem);
    font-weight: 900;
    letter-spacing: 0.3em;
    margin: 2rem 0;
    line-height: 1.2;
    position: relative;
    padding: 1.5rem 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 1rem;
}

.brand-word {
    display: flex;
    justify-content: center;
    align-items: center;
}

.brand-name::before {
    content: '';
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 120%;
    height: 100%;
    background: radial-gradient(ellipse at center, rgba(212, 175, 55, 0.08) 0%, transparent 70%);
    border-radius: 50%;
    z-index: -1;
    animation: brandGlowPulse 4s ease-in-out infinite;
}

@keyframes brandGlowPulse {
    0%, 100% {
        opacity: 0.3;
        transform: translateX(-50%) scale(1);
    }
    50% {
        opacity: 0.6;
        transform: translateX(-50%) scale(1.05);
    }
}

.brand-name .letter {
    display: inline-block;
    color: transparent;
    background: linear-gradient(135deg, 
        var(--champagne-gold) 0%, 
        var(--pearl-white) 50%, 
        var(--champagne-gold) 100%);
    background-size: 200% 200%;
    -webkit-background-clip: text;
    background-clip: text;
    text-shadow: none;
    filter: drop-shadow(0 0 20px rgba(212, 175, 55, 0.4))
            drop-shadow(0 4px 8px rgba(0, 0, 0, 0.5));
    animation: 
        letterFloat 4s ease-in-out infinite,
        gradientShift 6s ease-in-out infinite;
    animation-delay: calc(var(--i) * 0.1s);
    will-change: transform;
    position: relative;
    transform-style: preserve-3d;
}

.brand-name .letter::after {
    content: attr(data-letter);
    position: absolute;
    top: 3px;
    left: 3px;
    color: rgba(212, 175, 55, 0.15);
    z-index: -1;
    -webkit-background-clip: unset;
    background-clip: unset;
    filter: blur(2px);
}

.brand-word:nth-child(1) .letter:nth-child(1) { --i: 0; }
.brand-word:nth-child(1) .letter:nth-child(2) { --i: 1; }
.brand-word:nth-child(1) .letter:nth-child(3) { --i: 2; }
.brand-word:nth-child(1) .letter:nth-child(4) { --i: 3; }
.brand-word:nth-child(1) .letter:nth-child(5) { --i: 4; }
.brand-word:nth-child(1) .letter:nth-child(6) { --i: 5; }
.brand-word:nth-child(1) .letter:nth-child(7) { --i: 6; }
.brand-word:nth-child(2) .letter:nth-child(1) { --i: 7; }
.brand-word:nth-child(2) .letter:nth-child(2) { --i: 8; }
.brand-word:nth-child(2) .letter:nth-child(3) { --i: 9; }
.brand-word:nth-child(2) .letter:nth-child(4) { --i: 10; }

@keyframes letterFloat {
    0%, 100% {
        transform: translate3d(0, 0, 0) rotateY(0deg);
    }
    50% {
        transform: translate3d(0, -12px, 0) rotateY(5deg);
    }
}

@keyframes gradientShift {
    0%, 100% {
        background-position: 0% 50%;
    }
    50% {
        background-position: 100% 50%;
    }
}

.brand-divider {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    margin: 2rem 0;
}

.divider-ornament {
    width: 80px;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--champagne-gold), transparent);
}

.divider-diamond {
    font-size: 1.5rem;
    color: var(--champagne-gold);
    text-shadow: 0 0 20px var(--champagne-gold);
    animation: diamondRotate 4s linear infinite;
}

@keyframes diamondRotate {
    0%, 100% {
        transform: rotate(0deg) scale(1);
    }
    50% {
        transform: rotate(180deg) scale(1.2);
    }
}

.brand-tagline {
    font-family: var(--font-body);
    font-size: clamp(0.875rem, 2vw, 1.25rem);
    font-weight: 400;
    letter-spacing: 0.4em;
    color: var(--platinum);
    margin: 2rem 0;
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    flex-wrap: wrap;
}

.tagline-word {
    display: inline-block;
    opacity: 0;
    animation: wordFadeIn 1s ease-out forwards;
}

.tagline-word:nth-child(1) { animation-delay: 0.5s; }
.tagline-word:nth-child(2) { animation-delay: 0.8s; }
.tagline-word:nth-child(3) { animation-delay: 1.1s; }
.tagline-word:nth-child(4) { animation-delay: 1.4s; }

@keyframes wordFadeIn {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.scroll-indicator {
    margin-top: 4rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    opacity: 0;
    animation: scrollFadeIn 1s ease-out 2s forwards;
    cursor: pointer;
    padding: 1rem;
    transition: all 0.3s var(--ease-luxury);
}

.scroll-indicator:hover {
    transform: translateY(-5px);
}

.scroll-indicator:hover .scroll-text {
    color: var(--pearl-white);
    text-shadow: 0 0 25px rgba(212, 175, 55, 0.8);
}

.scroll-indicator:hover .scroll-arrow {
    animation: arrowBounceHover 0.6s ease-in-out infinite;
}

@keyframes scrollFadeIn {
    to {
        opacity: 1;
    }
}

.scroll-text {
    font-family: var(--font-display);
    font-size: 0.75rem;
    letter-spacing: 0.3em;
    color: var(--champagne-gold);
    transition: all 0.3s var(--ease-luxury);
}

.scroll-arrow {
    font-size: 1.5rem;
    color: var(--champagne-gold);
    animation: arrowBounce 2s ease-in-out infinite;
    text-shadow: 0 0 15px rgba(212, 175, 55, 0.5);
}

@keyframes arrowBounce {
    0%, 100% {
        transform: translateY(0);
    }
    50% {
        transform: translateY(10px);
    }
}

@keyframes arrowBounceHover {
    0%, 100% {
        transform: translateY(0) scale(1);
    }
    50% {
        transform: translateY(15px) scale(1.1);
    }
}

/* ============================================
   MENU SECTIONS
   ============================================ */
.menu-content {
    padding: var(--space-2xl) 0;
}

.menu-section {
    margin-bottom: var(--space-2xl);
    opacity: 0;
    transform: translateY(50px);
    transition: all 1s var(--ease-smooth);
}

.menu-section.visible {
    opacity: 1;
    transform: translateY(0);
}

.section-header {
    text-align: center;
    margin-bottom: var(--space-xl);
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    padding: 3rem 0;
}

.section-number {
    font-family: var(--font-display);
    font-size: 3rem;
    font-weight: 900;
    color: var(--champagne-gold);
    opacity: 0.2;
    letter-spacing: 0.1em;
}

.section-ornamental-line {
    flex: 1;
    max-width: 200px;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--champagne-gold), transparent);
    position: relative;
}

.section-ornamental-line::before,
.section-ornamental-line::after {
    content: '';
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 4px;
    height: 4px;
    background: var(--champagne-gold);
    border-radius: 50%;
    box-shadow: 0 0 10px var(--champagne-gold);
}

.section-ornamental-line::before { left: 20%; }
.section-ornamental-line::after { right: 20%; }

.section-title {
    text-align: center;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

.title-main {
    font-family: var(--font-display);
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 900;
    color: var(--champagne-gold);
    letter-spacing: 0.2em;
    text-shadow: 0 0 20px rgba(212, 175, 55, 0.3);
}

.title-subtitle {
    font-family: var(--font-body);
    font-size: clamp(1rem, 2vw, 1.5rem);
    font-weight: 400;
    color: var(--platinum);
    letter-spacing: 0.3em;
    font-style: italic;
}

/* ============================================
   MENU ITEMS
   ============================================ */
.menu-items {
    display: grid;
    gap: 3rem;
}

.menu-item {
    background: linear-gradient(135deg, 
        rgba(26, 26, 26, 0.6) 0%,
        rgba(42, 37, 32, 0.4) 100%);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    border-radius: 20px;
    padding: 3rem;
    position: relative;
    overflow: hidden;
    transition: all 0.4s var(--ease-smooth);
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
    will-change: transform;
    transform: translateZ(0); /* Force GPU acceleration */
}

.menu-item::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(212, 175, 55, 0.05) 0%, transparent 70%);
    opacity: 0;
    transition: opacity 0.4s var(--ease-luxury);
    pointer-events: none;
    will-change: opacity;
}

.menu-item:hover::before {
    opacity: 1;
}

.menu-item::after {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, 
        transparent 0%, 
        rgba(212, 175, 55, 0.1) 50%, 
        transparent 100%);
    transition: left 0.6s var(--ease-luxury);
    will-change: left;
}

.menu-item:hover::after {
    left: 100%;
}

.menu-item:hover {
    transform: translateY(-8px);
    border-color: rgba(212, 175, 55, 0.4);
    box-shadow: 
        0 20px 60px rgba(0, 0, 0, 0.4),
        0 0 40px rgba(212, 175, 55, 0.2),
        inset 0 1px 0 rgba(212, 175, 55, 0.2);
}

.item-luxury-badge {
    position: absolute;
    top: 2rem;
    right: 2rem;
    font-family: var(--font-display);
    font-size: 0.65rem;
    font-weight: 600;
    letter-spacing: 0.2em;
    color: var(--champagne-gold);
    padding: 0.5rem 1rem;
    background: rgba(212, 175, 55, 0.1);
    border: 1px solid rgba(212, 175, 55, 0.3);
    border-radius: 20px;
    backdrop-filter: blur(10px);
    text-shadow: 0 0 10px rgba(212, 175, 55, 0.5);
    z-index: 10;
}

.item-number {
    position: absolute;
    top: 2rem;
    left: 2rem;
    font-family: var(--font-display);
    font-size: 4rem;
    font-weight: 900;
    color: var(--champagne-gold);
    opacity: 0.05;
    line-height: 1;
    z-index: 1;
}

.item-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 1.5rem;
    position: relative;
    z-index: 5;
    gap: 1rem;
}

.item-name {
    flex: 1;
    display: flex;
    align-items: center;
    gap: 1rem;
}

.name-main {
    font-family: var(--font-accent);
    font-size: clamp(1.5rem, 3vw, 2rem);
    font-weight: 700;
    color: var(--pearl-white);
    letter-spacing: 0.05em;
    line-height: 1.2;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5);
}

.name-ornament {
    font-size: 1.5rem;
    color: var(--champagne-gold);
    text-shadow: 0 0 15px rgba(212, 175, 55, 0.6);
    transition: all 0.3s var(--ease-bounce);
    will-change: transform;
}

.menu-item:hover .name-ornament {
    transform: rotate(180deg) scale(1.2);
}

.item-price {
    font-size: 1.25rem;
    color: var(--champagne-gold);
    letter-spacing: 0.2em;
    text-shadow: 0 0 15px rgba(212, 175, 55, 0.5);
    white-space: nowrap;
}

.item-description {
    font-family: var(--font-body);
    font-size: 1.125rem;
    line-height: 1.9;
    color: rgba(248, 246, 240, 0.85);
    position: relative;
    z-index: 5;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.3);
    transition: color 0.4s var(--ease-luxury);
}

.menu-item:hover .item-description {
    color: rgba(248, 246, 240, 1);
}

/* Luxury tier specific styling */
.menu-item[data-luxury="royal"] {
    background: linear-gradient(135deg, 
        rgba(128, 0, 32, 0.2) 0%,
        rgba(42, 37, 32, 0.4) 100%);
}

.menu-item[data-luxury="royal"]:hover {
    border-color: rgba(128, 0, 32, 0.6);
}

.menu-item[data-luxury="premium"] {
    background: linear-gradient(135deg, 
        rgba(212, 175, 55, 0.1) 0%,
        rgba(42, 37, 32, 0.4) 100%);
}

.menu-item[data-luxury="exclusive"] {
    background: linear-gradient(135deg, 
        rgba(183, 110, 121, 0.15) 0%,
        rgba(42, 37, 32, 0.4) 100%);
}

/* Compact items for sides */
.menu-item-compact {
    padding: 2rem;
}

.menu-item-compact .item-description {
    font-size: 1rem;
}

.sides-grid {
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
}

/* ============================================
   FOOTER
   ============================================ */
.menu-footer {
    margin-top: var(--space-2xl);
    padding: var(--space-2xl) 0;
    border-top: 1px solid var(--glass-border);
    position: relative;
}

.footer-ornament-top {
    width: 100%;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--champagne-gold), transparent);
    margin-bottom: 3rem;
    position: relative;
}

.footer-ornament-top::before {
    content: '◆';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: var(--champagne-gold);
    font-size: 1rem;
    background: var(--deep-black);
    padding: 0 1rem;
    text-shadow: 0 0 20px var(--champagne-gold);
}

.brand-signature {
    text-align: center;
    margin-bottom: 3rem;
}

.signature-emblem {
    width: 80px;
    height: 80px;
    margin: 0 auto 2rem;
    border: 2px solid var(--champagne-gold);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    animation: emblemRotate 20s linear infinite;
}

@keyframes emblemRotate {
    from {
        transform: rotate(0deg);
    }
    to {
        transform: rotate(360deg);
    }
}

.emblem-text {
    font-family: var(--font-display);
    font-size: 1.5rem;
    font-weight: 900;
    color: var(--champagne-gold);
    letter-spacing: 0.1em;
}

.signature-text {
    font-family: var(--font-display);
    font-size: 1rem;
    font-weight: 600;
    letter-spacing: 0.3em;
    color: var(--platinum);
    margin-bottom: 1.5rem;
}

.signature-line-ornamental {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    margin: 1.5rem 0;
}

.line-dot {
    width: 4px;
    height: 4px;
    background: var(--champagne-gold);
    border-radius: 50%;
    box-shadow: 0 0 10px var(--champagne-gold);
}

.line {
    width: 60px;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--champagne-gold), transparent);
}

.line-diamond {
    color: var(--champagne-gold);
    font-size: 1rem;
    text-shadow: 0 0 20px var(--champagne-gold);
}

.signature-location {
    font-family: var(--font-body);
    font-size: 0.875rem;
    letter-spacing: 0.2em;
    color: var(--platinum);
    opacity: 0.7;
}

.footer-info {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 3rem;
    margin-top: 3rem;
    padding-top: 3rem;
    border-top: 1px solid var(--glass-border);
}

.footer-section h4 {
    font-family: var(--font-display);
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 0.2em;
    color: var(--champagne-gold);
    margin-bottom: 1rem;
}

.footer-section p {
    font-family: var(--font-body);
    font-size: 1rem;
    line-height: 1.8;
    color: var(--platinum);
    opacity: 0.8;
}

.footer-section p a,
.footer-section p a:link,
.footer-section p a:visited,
.footer-section p a:hover,
.footer-section p a:active {
    color: var(--platinum);
    text-decoration: none;
    opacity: 0.8;
}

/* ============================================
   MOBILE OPTIMIZATION - REDUCE ANIMATIONS
   ============================================ */
@media (max-width: 1024px) {
    /* Force hardware acceleration on mobile */
    * {
        -webkit-transform: translate3d(0, 0, 0);
        -webkit-backface-visibility: hidden;
        -webkit-perspective: 1000;
    }
    
    /* Disable parallax effects on mobile for better performance */
    .parallax-layer,
    .orb {
        animation: none !important;
        transform: translate3d(0, 0, 0) !important;
        will-change: auto !important;
    }
    
    /* Reduce hero animations */
    .brand-name .letter {
        animation: none !important;
        filter: drop-shadow(0 0 10px rgba(212, 175, 55, 0.3));
        will-change: auto;
        transform: translate3d(0, 0, 0);
    }
    
    /* Disable all ornament animations on mobile */
    .brand-ornament,
    .divider-diamond,
    .scroll-arrow {
        animation: none !important;
    }
    
    /* Optimize hero section for mobile */
    .hero-section {
        transform: translate3d(0, 0, 0) !important;
        will-change: auto;
    }
    
    .brand-name::before {
        animation: none !important;
    }
    
    /* Disable tagline animations on mobile */
    .tagline-word {
        animation: none !important;
        opacity: 1;
    }
    
    /* Disable scroll indicator animation on mobile */
    .scroll-indicator {
        animation: none !important;
        opacity: 1;
    }
    
    .luxury-nav {
        padding: 1.5rem 2rem;
    }
    
    .nav-links {
        gap: 2rem;
    }
    
    .menu-container {
        padding: 0 1.5rem;
        transform: translate3d(0, 0, 0);
    }
}

/* ============================================
   RESPONSIVE DESIGN
   ============================================ */

@media (max-width: 768px) {
    .luxury-nav {
        padding: 1rem 1.5rem;
    }
    
    .mobile-menu-toggle {
        display: flex;
    }
    
    .nav-links {
        position: fixed;
        top: 0;
        right: -100%;
        width: 70%;
        max-width: 300px;
        height: 100vh;
        background: rgba(10, 10, 10, 0.98);
        backdrop-filter: blur(20px);
        flex-direction: column;
        justify-content: center;
        align-items: center;
        gap: 2.5rem;
        transition: right 0.4s var(--ease-luxury);
        border-left: 1px solid var(--glass-border);
        z-index: 1000;
    }
    
    .nav-links.active {
        right: 0;
    }
    
    .nav-link {
        font-size: 1rem;
        padding: 1rem 0;
    }
    
    .hero-section {
        min-height: 80vh;
        margin-top: 60px;
    }
    
    .brand-identity {
        padding: 2rem 1rem;
    }
    
    .brand-name {
        letter-spacing: 0.2em;
        font-size: clamp(2.5rem, 10vw, 4rem) !important;
        gap: 0.5rem;
    }
    
    .brand-tagline {
        letter-spacing: 0.15em;
        gap: 0.8rem;
        font-size: 0.75rem !important;
    }
    
    .brand-ornament {
        width: 100px;
    }
    
    .section-header {
        flex-direction: column;
        gap: 1.5rem;
        padding: 2rem 0;
    }
    
    .section-number {
        font-size: 2rem;
    }
    
    .section-ornamental-line {
        max-width: 120px;
    }
    
    .title-main {
        font-size: clamp(1.75rem, 8vw, 2.5rem) !important;
    }
    
    .title-subtitle {
        font-size: clamp(0.875rem, 3vw, 1.25rem) !important;
    }
    
    .menu-items {
        gap: 2rem;
    }
    
    .menu-item {
        padding: 1.75rem 1.25rem;
        border-radius: 16px;
    }
    
    .item-luxury-badge {
        top: 1rem;
        right: 1rem;
        font-size: 0.55rem;
        padding: 0.35rem 0.75rem;
    }
    
    .item-number {
        font-size: 2.5rem;
        top: 1rem;
        left: 1rem;
    }
    
    .item-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.75rem;
        margin-bottom: 1rem;
    }
    
    .item-name {
        width: 100%;
    }
    
    .name-main {
        font-size: clamp(1.25rem, 5vw, 1.75rem) !important;
    }
    
    .item-price {
        align-self: flex-start;
        font-size: 1rem;
    }
    
    .item-description {
        font-size: 1rem;
        line-height: 1.75;
    }
    
    .footer-info {
        grid-template-columns: 1fr;
        gap: 2rem;
    }
    
    .footer-section p {
        font-size: 1.125rem;
    }
}

@media (max-width: 480px) {
    .menu-container {
        padding: 0 1rem;
    }
    
    .luxury-nav {
        padding: 1rem;
    }
    
    .nav-logo {
        font-size: 1.5rem;
    }
    
    .nav-links {
        width: 80%;
        gap: 2rem;
    }
    
    .nav-link {
        font-size: 0.875rem;
    }
    
    .hero-section {
        min-height: 70vh;
    }
    
    .brand-name {
        letter-spacing: 0.15em;
        font-size: clamp(2rem, 9vw, 2.8rem) !important;
        margin: 1.5rem 0;
        gap: 0.3rem;
    }
    
    .brand-divider {
        gap: 1rem;
    }
    
    .divider-ornament {
        width: 50px;
    }
    
    .scroll-indicator {
        margin-top: 2rem;
    }
    
    .menu-content {
        padding: 3rem 0;
    }
    
    .menu-section {
        margin-bottom: 3rem;
    }
    
    .section-header {
        padding: 1.5rem 0;
    }
    
    .menu-item {
        padding: 1.5rem 1rem;
        border-radius: 12px;
    }
    
    .item-luxury-badge {
        font-size: 0.5rem;
        padding: 0.3rem 0.6rem;
    }
    
    .item-number {
        font-size: 2rem;
    }
    
    .name-main {
        font-size: clamp(1.125rem, 6vw, 1.5rem) !important;
    }
    
    .name-ornament {
        font-size: 1.25rem;
    }
    
    .item-description {
        font-size: 0.9375rem;
        line-height: 1.7;
    }
    
    .sides-grid {
        grid-template-columns: 1fr;
        gap: 1.5rem;
    }
    
    .menu-item-compact {
        padding: 1.25rem 1rem;
    }
    
    .footer-info {
        gap: 1.5rem;
    }
    
    .footer-section h4 {
        font-size: 0.75rem;
    }
    
    .footer-section p {
        font-size: 1.0625rem;
    }
}

/* Extra small devices - Mobile-first optimization */
@media (max-width: 375px) {
    .brand-name {
        letter-spacing: 0.1em;
        font-size: clamp(1.8rem, 8.5vw, 2.4rem) !important;
        gap: 0.2rem;
    }
    
    .menu-item {
        padding: 1.25rem 0.875rem;
    }
    
    .item-description {
        font-size: 0.875rem;
    }
    
    .brand-tagline {
        gap: 0.5rem;
    }
}

/* Landscape mode optimization for mobile */
@media (max-height: 600px) and (orientation: landscape) {
    .hero-section {
        min-height: 100vh;
        padding: 2rem 0;
    }
    
    .brand-identity {
        padding: 1rem;
    }
    
    .brand-ornament.top {
        margin-bottom: 1rem;
    }
    
    .brand-ornament.bottom {
        margin-top: 1rem;
    }
    
    .brand-name {
        margin: 1rem 0;
    }
    
    .scroll-indicator {
        display: none;
    }
}

/* ============================================
   LUXURIOUS LOADING SCREEN
   ============================================ */
.loading-screen {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
    background: linear-gradient(135deg, var(--deep-black) 0%, var(--warm-black) 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10000;
    opacity: 1;
    visibility: visible;
    transition: opacity 1s ease-out, visibility 1s ease-out;
}

.loading-screen.hidden {
    opacity: 0;
    visibility: hidden;
}

.loading-content {
    text-align: center;
    padding: 2rem;
    max-width: 600px;
}

.logo-container {
    margin-bottom: 3rem;
    animation: logoReveal 2s ease-out;
}

@keyframes logoReveal {
    0% {
        opacity: 0;
        transform: scale(0.8) translateY(-30px);
    }
    60% {
        opacity: 1;
        transform: scale(1.05) translateY(0);
    }
    100% {
        opacity: 1;
        transform: scale(1) translateY(0);
    }
}

.loading-logo {
    width: 100%;
    max-width: 400px;
    height: auto;
    filter: drop-shadow(0 0 40px rgba(212, 175, 55, 0.4));
    animation: logoGlow 3s ease-in-out infinite;
}

@keyframes logoGlow {
    0%, 100% {
        filter: drop-shadow(0 0 40px rgba(212, 175, 55, 0.4));
    }
    50% {
        filter: drop-shadow(0 0 60px rgba(212, 175, 55, 0.7));
    }
}

.loading-ornament {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    margin: 2rem 0;
    opacity: 0;
    animation: ornamentFadeIn 1s ease-out 0.5s forwards;
}

@keyframes ornamentFadeIn {
    to {
        opacity: 1;
    }
}

.ornament-dot {
    width: 4px;
    height: 4px;
    background: var(--champagne-gold);
    border-radius: 50%;
    box-shadow: 0 0 10px var(--champagne-gold);
    animation: dotPulse 2s ease-in-out infinite;
}

@keyframes dotPulse {
    0%, 100% {
        opacity: 0.5;
        transform: scale(1);
    }
    50% {
        opacity: 1;
        transform: scale(1.3);
    }
}

.ornament-line {
    width: 80px;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--champagne-gold), transparent);
}

.ornament-diamond {
    font-size: 1.5rem;
    color: var(--champagne-gold);
    text-shadow: 0 0 20px var(--champagne-gold);
    animation: diamondSpin 4s linear infinite;
}

@keyframes diamondSpin {
    0%, 100% {
        transform: rotate(0deg) scale(1);
    }
    50% {
        transform: rotate(180deg) scale(1.2);
    }
}

.loading-text {
    font-family: var(--font-display);
    font-size: clamp(0.75rem, 1.8vw, 1.1rem);
    font-weight: 600;
    letter-spacing: 0.3em;
    color: var(--platinum);
    margin: 2rem 0;
    display: flex;
    justify-content: center;
    flex-wrap: nowrap;
    gap: 0.3rem;
    white-space: nowrap;
}

.loading-letter {
    display: inline-block;
    opacity: 0;
    animation: letterReveal 0.5s ease-out forwards;
}

.loading-letter:nth-child(1) { animation-delay: 0.8s; }
.loading-letter:nth-child(2) { animation-delay: 0.9s; }
.loading-letter:nth-child(3) { animation-delay: 1.0s; }
.loading-letter:nth-child(4) { animation-delay: 1.1s; }
.loading-letter:nth-child(5) { animation-delay: 1.2s; }
.loading-letter:nth-child(6) { animation-delay: 1.3s; }
.loading-letter:nth-child(7) { animation-delay: 1.4s; }
.loading-letter:nth-child(8) { animation-delay: 1.5s; }
.loading-letter:nth-child(9) { animation-delay: 1.6s; }
.loading-letter:nth-child(11) { animation-delay: 1.7s; }
.loading-letter:nth-child(12) { animation-delay: 1.8s; }
.loading-letter:nth-child(13) { animation-delay: 1.9s; }
.loading-letter:nth-child(14) { animation-delay: 2.0s; }
.loading-letter:nth-child(16) { animation-delay: 2.1s; }
.loading-letter:nth-child(17) { animation-delay: 2.2s; }
.loading-letter:nth-child(18) { animation-delay: 2.3s; }
.loading-letter:nth-child(19) { animation-delay: 2.4s; }
.loading-letter:nth-child(20) { animation-delay: 2.5s; }
.loading-letter:nth-child(21) { animation-delay: 2.6s; }
.loading-letter:nth-child(22) { animation-delay: 2.7s; }
.loading-letter:nth-child(23) { animation-delay: 2.8s; }
.loading-letter:nth-child(24) { animation-delay: 2.9s; }
.loading-letter:nth-child(25) { animation-delay: 3.0s; }

@keyframes letterReveal {
    0% {
        opacity: 0;
        transform: translateY(20px);
    }
    100% {
        opacity: 1;
        transform: translateY(0);
    }
}

.loading-space {
    display: inline-block;
    width: 0.5rem;
}

.loading-progress {
    width: 100%;
    max-width: 400px;
    height: 3px;
    background: rgba(212, 175, 55, 0.1);
    border-radius: 10px;
    margin: 3rem auto 0;
    overflow: hidden;
    opacity: 0;
    animation: progressFadeIn 0.5s ease-out 1s forwards;
}

@keyframes progressFadeIn {
    to {
        opacity: 1;
    }
}

.progress-bar {
    height: 100%;
    background: linear-gradient(90deg, 
        var(--champagne-gold) 0%, 
        var(--rose-gold) 50%, 
        var(--champagne-gold) 100%);
    background-size: 200% 100%;
    width: 0;
    border-radius: 10px;
    box-shadow: 0 0 20px rgba(212, 175, 55, 0.6);
    animation: progressFill 3s ease-out 1.2s forwards,
               progressGlow 2s ease-in-out 1.2s infinite;
}

@keyframes progressFill {
    to {
        width: 100%;
    }
}

@keyframes progressGlow {
    0%, 100% {
        background-position: 0% 50%;
        box-shadow: 0 0 20px rgba(212, 175, 55, 0.6);
    }
    50% {
        background-position: 100% 50%;
        box-shadow: 0 0 30px rgba(212, 175, 55, 0.9);
    }
}

/* Loading screen responsive adjustments */
@media (max-width: 768px) {
    .loading-logo {
        max-width: 300px;
    }
    
    .loading-text {
        font-size: 0.65rem;
        letter-spacing: 0.15em;
        gap: 0.2rem;
    }
    
    .ornament-line {
        width: 60px;
    }
    
    .loading-progress {
        max-width: 300px;
    }
}

@media (max-width: 480px) {
    .loading-logo {
        max-width: 250px;
    }
    
    .logo-container {
        margin-bottom: 2rem;
    }
    
    .loading-ornament {
        gap: 0.5rem;
        margin: 1.5rem 0;
    }
    
    .ornament-line {
        width: 40px;
    }
    
    .ornament-diamond {
        font-size: 1.2rem;
    }
    
    .loading-text {
        font-size: 0.55rem;
        gap: 0.15rem;
        letter-spacing: 0.1em;
    }
    
    .loading-progress {
        max-width: 250px;
        margin-top: 2rem;
    }
}

/* ============================================
   SCROLL TO TOP BUTTON
   ============================================ */
.scroll-to-top {
    position: fixed;
    bottom: 2rem;
    right: 2rem;
    width: 56px;
    height: 56px;
    background: linear-gradient(135deg, 
        rgba(212, 175, 55, 0.9) 0%, 
        rgba(183, 110, 121, 0.9) 100%);
    border: 2px solid var(--champagne-gold);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    z-index: 999;
    opacity: 0;
    visibility: hidden;
    transform: translateY(20px) scale(0.8);
    transition: all 0.4s var(--ease-smooth);
    box-shadow: 
        0 10px 30px rgba(0, 0, 0, 0.4),
        0 0 20px rgba(212, 175, 55, 0.3);
    backdrop-filter: blur(10px);
    will-change: transform, opacity;
}

.scroll-to-top.visible {
    opacity: 1;
    visibility: visible;
    transform: translateY(0) scale(1);
}

.scroll-to-top:hover {
    transform: translateY(-5px) scale(1.1);
    box-shadow: 
        0 15px 40px rgba(0, 0, 0, 0.5),
        0 0 30px rgba(212, 175, 55, 0.6);
    border-color: var(--pearl-white);
}

.scroll-to-top:active {
    transform: translateY(-2px) scale(1.05);
}

.scroll-to-top svg {
    width: 24px;
    height: 24px;
    color: var(--pearl-white);
    filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.3));
    transition: all 0.3s var(--ease-luxury);
}

.scroll-to-top:hover svg {
    transform: translateY(-3px);
    filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.5));
}

/* Responsive adjustments for scroll-to-top */
@media (max-width: 768px) {
    .scroll-to-top {
        width: 50px;
        height: 50px;
        bottom: 1.5rem;
        right: 1.5rem;
    }
    
    .scroll-to-top svg {
        width: 20px;
        height: 20px;
    }
}

@media (max-width: 480px) {
    .scroll-to-top {
        width: 44px;
        height: 44px;
        bottom: 1rem;
        right: 1rem;
    }
    
    .scroll-to-top svg {
        width: 18px;
        height: 18px;
    }
}

/* ============================================
   LOGIN PAGE STYLES
   ============================================ */
.login-page {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
    position: relative;
    background: var(--deep-black);
}

.login-container {
    width: 100%;
    max-width: 500px;
    position: relative;
    z-index: 10;
}

.login-card {
    background: linear-gradient(135deg, 
        rgba(26, 26, 26, 0.95) 0%,
        rgba(42, 37, 32, 0.95) 100%);
    backdrop-filter: blur(30px);
    border: 1px solid var(--glass-border);
    border-radius: 24px;
    padding: 4rem 3rem;
    box-shadow: 
        0 20px 60px rgba(0, 0, 0, 0.5),
        inset 0 1px 0 rgba(212, 175, 55, 0.1);
    position: relative;
    overflow: hidden;
}

.login-card::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(212, 175, 55, 0.05) 0%, transparent 70%);
    animation: loginGlow 8s ease-in-out infinite;
}

@keyframes loginGlow {
    0%, 100% {
        transform: translate(0, 0);
    }
    50% {
        transform: translate(30px, 30px);
    }
}

.login-header {
    text-align: center;
    margin-bottom: 3rem;
    position: relative;
    z-index: 2;
}

.login-ornament {
    width: 100px;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--champagne-gold), transparent);
    margin: 0 auto 2rem;
    position: relative;
}

.login-ornament::before,
.login-ornament::after {
    content: '◆';
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    color: var(--champagne-gold);
    font-size: 0.75rem;
    text-shadow: 0 0 15px var(--champagne-gold);
}

.login-ornament::before { left: -15px; }
.login-ornament::after { right: -15px; }

.login-title {
    font-family: var(--font-display);
    font-size: clamp(2rem, 5vw, 2.75rem);
    font-weight: 900;
    color: var(--champagne-gold);
    letter-spacing: 0.15em;
    margin-bottom: 1rem;
    text-shadow: 0 0 30px rgba(212, 175, 55, 0.4);
}

.login-subtitle {
    font-family: var(--font-body);
    font-size: 1.125rem;
    color: var(--platinum);
    letter-spacing: 0.1em;
    opacity: 0.9;
}

.login-divider {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    margin: 2.5rem 0;
    position: relative;
    z-index: 2;
}

.divider-line {
    flex: 1;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--champagne-gold), transparent);
}

.divider-text {
    font-family: var(--font-display);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.2em;
    color: var(--champagne-gold);
    text-transform: uppercase;
}

.login-form {
    position: relative;
    z-index: 2;
}

.google-sign-in-btn {
    width: 100%;
    padding: 1.25rem 2rem;
    background: linear-gradient(135deg, 
        rgba(212, 175, 55, 0.95) 0%, 
        rgba(183, 110, 121, 0.95) 100%);
    border: 2px solid var(--champagne-gold);
    border-radius: 50px;
    font-family: var(--font-display);
    font-size: 1rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    color: var(--pearl-white);
    cursor: pointer;
    transition: all 0.4s var(--ease-smooth);
    box-shadow: 
        0 10px 30px rgba(0, 0, 0, 0.4),
        0 0 30px rgba(212, 175, 55, 0.3);
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    position: relative;
    overflow: hidden;
}

.google-sign-in-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, 
        transparent 0%, 
        rgba(255, 255, 255, 0.3) 50%, 
        transparent 100%);
    transition: left 0.6s var(--ease-luxury);
}

.google-sign-in-btn:hover::before {
    left: 100%;
}

.google-sign-in-btn:hover {
    transform: translateY(-3px) scale(1.02);
    box-shadow: 
        0 15px 40px rgba(0, 0, 0, 0.5),
        0 0 50px rgba(212, 175, 55, 0.6);
    border-color: var(--pearl-white);
}

.google-sign-in-btn:active {
    transform: translateY(-1px) scale(1);
}

.google-icon {
    width: 20px;
    height: 20px;
    filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.3));
}

.login-features {
    margin-top: 3rem;
    padding-top: 3rem;
    border-top: 1px solid rgba(212, 175, 55, 0.2);
    position: relative;
    z-index: 2;
}

.features-title {
    font-family: var(--font-display);
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 0.2em;
    color: var(--champagne-gold);
    text-align: center;
    margin-bottom: 1.5rem;
    text-transform: uppercase;
}

.features-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
}

.feature-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    font-family: var(--font-body);
    font-size: 1rem;
    color: var(--platinum);
    opacity: 0.85;
}

.feature-icon {
    color: var(--champagne-gold);
    font-size: 1.25rem;
    text-shadow: 0 0 10px rgba(212, 175, 55, 0.5);
}

/* ============================================
   PROFILE PAGE STYLES
   ============================================ */
.profile-page {
    min-height: 100vh;
    padding: 120px 2rem 4rem;
    background: var(--deep-black);
    position: relative;
}

.profile-container {
    max-width: 1200px;
    margin: 0 auto;
    position: relative;
    z-index: 10;
}

.profile-welcome {
    text-align: center;
    margin-bottom: 4rem;
    padding: 3rem 2rem;
    background: linear-gradient(135deg, 
        rgba(26, 26, 26, 0.7) 0%,
        rgba(42, 37, 32, 0.5) 100%);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    border-radius: 24px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
}

.welcome-ornament {
    width: 120px;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--champagne-gold), transparent);
    margin: 0 auto 2rem;
    position: relative;
}

.welcome-ornament::before,
.welcome-ornament::after {
    content: '◆';
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    color: var(--champagne-gold);
    font-size: 0.875rem;
    text-shadow: 0 0 20px var(--champagne-gold);
}

.welcome-ornament::before { left: -20px; }
.welcome-ornament::after { right: -20px; }

.welcome-title {
    font-family: var(--font-display);
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-weight: 900;
    color: var(--champagne-gold);
    letter-spacing: 0.15em;
    margin-bottom: 0.5rem;
    text-shadow: 0 0 30px rgba(212, 175, 55, 0.4);
}

.welcome-subtitle {
    font-family: var(--font-body);
    font-size: 1.25rem;
    color: var(--platinum);
    letter-spacing: 0.05em;
    opacity: 0.9;
}

.profile-content {
    display: grid;
    grid-template-columns: 280px 1fr;
    gap: 3rem;
}

.profile-sidebar {
    position: sticky;
    top: 100px;
    height: fit-content;
}

.profile-nav {
    background: linear-gradient(135deg, 
        rgba(26, 26, 26, 0.9) 0%,
        rgba(42, 37, 32, 0.8) 100%);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    border-radius: 20px;
    padding: 2rem;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
}

.nav-item {
    padding: 1rem 1.5rem;
    margin-bottom: 0.5rem;
    border-radius: 12px;
    font-family: var(--font-display);
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: var(--platinum);
    cursor: pointer;
    transition: all 0.3s var(--ease-luxury);
    border: 1px solid transparent;
}

.nav-item:hover {
    background: rgba(212, 175, 55, 0.1);
    border-color: rgba(212, 175, 55, 0.3);
    color: var(--champagne-gold);
    transform: translateX(5px);
}

.nav-item.active {
    background: linear-gradient(135deg, 
        rgba(212, 175, 55, 0.2) 0%,
        rgba(183, 110, 121, 0.2) 100%);
    border-color: var(--champagne-gold);
    color: var(--champagne-gold);
    box-shadow: 0 5px 20px rgba(212, 175, 55, 0.2);
}

.profile-main {
    min-height: 500px;
}

.profile-section {
    background: linear-gradient(135deg, 
        rgba(26, 26, 26, 0.9) 0%,
        rgba(42, 37, 32, 0.8) 100%);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    border-radius: 20px;
    padding: 3rem;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
    display: none;
}

.profile-section.active {
    display: block;
    animation: sectionFadeIn 0.5s ease-out;
}

@keyframes sectionFadeIn {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.section-header-profile {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 3rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid rgba(212, 175, 55, 0.2);
}

.section-title-profile {
    font-family: var(--font-display);
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--champagne-gold);
    letter-spacing: 0.1em;
}

.form-actions {
    display: flex;
    gap: 1rem;
}

.btn-edit,
.btn-save,
.btn-cancel,
.btn-sign-out {
    padding: 0.75rem 2rem;
    border-radius: 50px;
    font-family: var(--font-display);
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    cursor: pointer;
    transition: all 0.3s var(--ease-luxury);
    border: 2px solid;
}

.btn-edit,
.btn-save {
    background: linear-gradient(135deg, var(--champagne-gold), var(--rose-gold));
    border-color: var(--champagne-gold);
    color: var(--pearl-white);
    box-shadow: 0 5px 20px rgba(212, 175, 55, 0.3);
}

.btn-edit:hover,
.btn-save:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 30px rgba(212, 175, 55, 0.5);
}

.btn-cancel {
    background: transparent;
    border-color: var(--platinum);
    color: var(--platinum);
}

.btn-cancel:hover {
    background: rgba(229, 228, 226, 0.1);
    transform: translateY(-3px);
}

.btn-sign-out {
    background: transparent;
    border-color: var(--burgundy);
    color: var(--burgundy);
}

.btn-sign-out:hover {
    background: rgba(128, 0, 32, 0.2);
    transform: translateY(-3px);
    box-shadow: 0 5px 20px rgba(128, 0, 32, 0.3);
}

.form-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
}

.form-group-profile {
    margin-bottom: 2rem;
}

.form-label-profile {
    display: block;
    font-family: var(--font-display);
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: var(--champagne-gold);
    margin-bottom: 0.75rem;
    text-transform: uppercase;
}

.form-input-profile {
    width: 100%;
    padding: 1rem;
    background: rgba(26, 26, 26, 0.6);
    border: 1px solid rgba(212, 175, 55, 0.2);
    border-radius: 12px;
    font-family: var(--font-body);
    font-size: 1rem;
    color: var(--pearl-white);
    transition: all 0.3s var(--ease-luxury);
}

.form-input-profile:focus {
    outline: none;
    border-color: var(--champagne-gold);
    box-shadow: 0 0 20px rgba(212, 175, 55, 0.2);
}

.form-input-profile:disabled {
    opacity: 0.6;
    cursor: not-allowed;
}

.empty-state {
    text-align: center;
    padding: 4rem 2rem;
}

.empty-icon {
    font-size: 4rem;
    color: var(--champagne-gold);
    opacity: 0.3;
    margin-bottom: 1.5rem;
}

.empty-text {
    font-family: var(--font-body);
    font-size: 1.125rem;
    color: var(--platinum);
    opacity: 0.7;
}

.order-list,
.favorites-list {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
}

.order-item,
.favorite-item {
    padding: 2rem;
    background: rgba(26, 26, 26, 0.5);
    border: 1px solid rgba(212, 175, 55, 0.15);
    border-radius: 16px;
    transition: all 0.3s var(--ease-luxury);
}

.order-item:hover,
.favorite-item:hover {
    border-color: rgba(212, 175, 55, 0.4);
    transform: translateX(5px);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
}

.preferences-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 1.5rem;
}

.preference-tag {
    padding: 1.25rem;
    background: rgba(26, 26, 26, 0.6);
    border: 2px solid rgba(212, 175, 55, 0.2);
    border-radius: 16px;
    font-family: var(--font-display);
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: var(--platinum);
    cursor: pointer;
    transition: all 0.3s var(--ease-luxury);
    text-align: center;
    text-transform: uppercase;
}

.preference-tag:hover {
    border-color: rgba(212, 175, 55, 0.5);
    transform: translateY(-3px);
}

.preference-tag.active {
    background: linear-gradient(135deg, 
        rgba(212, 175, 55, 0.3) 0%,
        rgba(183, 110, 121, 0.3) 100%);
    border-color: var(--champagne-gold);
    color: var(--champagne-gold);
    box-shadow: 0 5px 20px rgba(212, 175, 55, 0.3);
}

/* Responsive adjustments */
@media (max-width: 1024px) {
    .profile-content {
        grid-template-columns: 1fr;
        gap: 2rem;
    }
    
    .profile-sidebar {
        position: static;
    }
    
    .profile-nav {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
        gap: 1rem;
        padding: 1.5rem;
    }
    
    .nav-item {
        padding: 1rem;
        margin-bottom: 0;
        text-align: center;
    }
}

@media (max-width: 768px) {
    .profile-page {
        padding: 100px 1.5rem 3rem;
    }
    
    .profile-welcome {
        padding: 2rem 1.5rem;
    }
    
    .profile-section {
        padding: 2rem 1.5rem;
    }
    
    .section-header-profile {
        flex-direction: column;
        align-items: flex-start;
        gap: 1.5rem;
    }
    
    .form-actions {
        width: 100%;
        flex-direction: column;
    }
    
    .btn-edit,
    .btn-save,
    .btn-cancel,
    .btn-sign-out {
        width: 100%;
    }
    
    .form-grid {
        grid-template-columns: 1fr;
        gap: 1.5rem;
    }
    
    .preferences-grid {
        grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
        gap: 1rem;
    }
    
    .login-card {
        padding: 3rem 2rem;
    }
}

@media (max-width: 480px) {
    .profile-welcome {
        padding: 1.5rem 1rem;
    }
    
    .profile-section {
        padding: 1.5rem 1rem;
    }
    
    .login-card {
        padding: 2rem 1.5rem;
    }
    
    .login-features {
        margin-top: 2rem;
        padding-top: 2rem;
    }
    
    .preferences-grid {
        grid-template-columns: 1fr;
    }
}

/* ============================================
   ACCESSIBILITY & PERFORMANCE
   ============================================ */
@media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
    }
    
    .menu-section {
        opacity: 1;
        transform: none;
    }
}

/* Focus states for accessibility */
a:focus,
button:focus {
    outline: 2px solid var(--champagne-gold);
    outline-offset: 4px;
}

/* Print styles */
@media print {
    .luxury-nav,
    .gradient-orbs,
    #particles-canvas,
    .scroll-indicator {
        display: none;
    }
    
    .menu-item {
        break-inside: avoid;
        border: 1px solid #ddd;
        background: white;
        color: black;
    }
}

/* ============================================
   LUXURIOUS CONTACT FORM SECTION
   ============================================ */
.contact-section {
    position: relative;
    padding: var(--space-2xl) 0;
    margin-top: var(--space-2xl);
    background: linear-gradient(135deg, 
        rgba(26, 26, 26, 0.8) 0%,
        rgba(42, 37, 32, 0.6) 50%,
        rgba(26, 26, 26, 0.8) 100%);
    backdrop-filter: blur(30px);
    border-top: 1px solid var(--glass-border);
    border-bottom: 1px solid var(--glass-border);
    overflow: hidden;
}

.contact-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: 
        radial-gradient(circle at 20% 30%, rgba(212, 175, 55, 0.08) 0%, transparent 50%),
        radial-gradient(circle at 80% 70%, rgba(183, 110, 121, 0.08) 0%, transparent 50%);
    pointer-events: none;
    z-index: 1;
}

.contact-container {
    position: relative;
    z-index: 2;
    max-width: 900px;
    margin: 0 auto;
    padding: 0 2rem;
}

.contact-header {
    text-align: center;
    margin-bottom: var(--space-xl);
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    padding: 2rem 0;
}

.contact-title {
    text-align: center;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

.contact-title .title-main {
    font-family: var(--font-display);
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 900;
    color: var(--champagne-gold);
    letter-spacing: 0.2em;
    text-shadow: 0 0 30px rgba(212, 175, 55, 0.4);
}

.contact-title .title-subtitle {
    font-family: var(--font-body);
    font-size: clamp(1rem, 2vw, 1.25rem);
    font-weight: 400;
    color: var(--platinum);
    letter-spacing: 0.2em;
    font-style: italic;
}

.contact-form {
    background: linear-gradient(135deg, 
        rgba(26, 26, 26, 0.7) 0%,
        rgba(42, 37, 32, 0.5) 100%);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    border-radius: 24px;
    padding: 3rem;
    box-shadow: 
        0 20px 60px rgba(0, 0, 0, 0.4),
        inset 0 1px 0 rgba(212, 175, 55, 0.1);
    position: relative;
    overflow: hidden;
}

.contact-form::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(212, 175, 55, 0.03) 0%, transparent 70%);
    pointer-events: none;
    animation: formGlow 8s ease-in-out infinite;
}

@keyframes formGlow {
    0%, 100% {
        transform: translate3d(0, 0, 0);
    }
    50% {
        transform: translate3d(30px, 30px, 0);
    }
}

.form-row {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    margin-bottom: 2rem;
}

.form-group {
    position: relative;
    margin-bottom: 2rem;
}

.form-label {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
    font-family: var(--font-display);
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    color: var(--champagne-gold);
    text-transform: uppercase;
}

.label-text {
    flex: 1;
}

.label-ornament {
    font-size: 0.75rem;
    color: var(--champagne-gold);
    text-shadow: 0 0 10px rgba(212, 175, 55, 0.5);
    animation: ornamentPulse 3s ease-in-out infinite;
}

@keyframes ornamentPulse {
    0%, 100% {
        opacity: 0.6;
        transform: scale(1);
    }
    50% {
        opacity: 1;
        transform: scale(1.2);
    }
}

.form-input,
.form-textarea {
    width: 100%;
    padding: 1rem 0;
    background: transparent;
    border: none;
    border-bottom: 2px solid rgba(212, 175, 55, 0.2);
    font-family: var(--font-body);
    font-size: 1.125rem;
    color: var(--pearl-white);
    letter-spacing: 0.02em;
    transition: all 0.4s var(--ease-luxury);
    outline: none;
}

.form-input::placeholder,
.form-textarea::placeholder {
    color: rgba(229, 228, 226, 0.4);
    font-style: italic;
}

.form-input:focus,
.form-textarea:focus {
    border-bottom-color: var(--champagne-gold);
}

.form-input:focus + .input-underline,
.form-textarea:focus + .input-underline {
    transform: scaleX(1);
}

.input-underline {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, 
        transparent 0%, 
        var(--champagne-gold) 50%, 
        transparent 100%);
    transform: scaleX(0);
    transition: transform 0.4s var(--ease-luxury);
    box-shadow: 0 0 20px rgba(212, 175, 55, 0.6);
}

.form-textarea {
    resize: vertical;
    min-height: 120px;
    padding-top: 0.75rem;
}

.form-submit-container {
    display: flex;
    justify-content: center;
    margin-top: 3rem;
}

.form-submit-btn {
    position: relative;
    padding: 1.25rem 4rem;
    background: linear-gradient(135deg, 
        rgba(212, 175, 55, 0.9) 0%, 
        rgba(183, 110, 121, 0.9) 100%);
    border: 2px solid var(--champagne-gold);
    border-radius: 50px;
    font-family: var(--font-display);
    font-size: 1rem;
    font-weight: 600;
    letter-spacing: 0.2em;
    color: var(--pearl-white);
    cursor: pointer;
    overflow: hidden;
    transition: all 0.4s var(--ease-smooth);
    box-shadow: 
        0 10px 30px rgba(0, 0, 0, 0.4),
        0 0 30px rgba(212, 175, 55, 0.3),
        inset 0 1px 0 rgba(255, 255, 255, 0.2);
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
}

.form-submit-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, 
        transparent 0%, 
        rgba(255, 255, 255, 0.3) 50%, 
        transparent 100%);
    transition: left 0.6s var(--ease-luxury);
}

.form-submit-btn:hover::before {
    left: 100%;
}

.form-submit-btn:hover {
    transform: translateY(-5px) scale(1.05);
    box-shadow: 
        0 15px 40px rgba(0, 0, 0, 0.5),
        0 0 50px rgba(212, 175, 55, 0.6),
        inset 0 1px 0 rgba(255, 255, 255, 0.3);
    border-color: var(--pearl-white);
}

.form-submit-btn:active {
    transform: translateY(-2px) scale(1.02);
}

.btn-text {
    position: relative;
    z-index: 2;
}

.btn-ornament {
    margin-left: 0.75rem;
    font-size: 1rem;
    animation: btnOrnamentSpin 4s linear infinite;
}

@keyframes btnOrnamentSpin {
    from {
        transform: rotate(0deg);
    }
    to {
        transform: rotate(360deg);
    }
}

.btn-shine {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(45deg, 
        transparent 0%, 
        rgba(255, 255, 255, 0.1) 50%, 
        transparent 100%);
    transform: translateX(-100%);
    animation: btnShine 3s ease-in-out infinite;
    pointer-events: none;
}

@keyframes btnShine {
    0%, 100% {
        transform: translateX(-100%);
    }
    50% {
        transform: translateX(100%);
    }
}

.form-message {
    margin-top: 2rem;
    padding: 1rem 2rem;
    border-radius: 12px;
    font-family: var(--font-body);
    font-size: 1rem;
    text-align: center;
    letter-spacing: 0.05em;
    opacity: 0;
    transform: translateY(-10px);
    transition: all 0.4s var(--ease-smooth);
}

.form-message.visible {
    opacity: 1;
    transform: translateY(0);
}

.form-message.success {
    background: rgba(45, 80, 22, 0.3);
    border: 1px solid rgba(45, 80, 22, 0.5);
    color: #90ee90;
}

.form-message.error {
    background: rgba(128, 0, 32, 0.3);
    border: 1px solid rgba(128, 0, 32, 0.5);
    color: #ff6b6b;
}

/* Loading state for submit button */
.form-submit-btn.loading {
    pointer-events: none;
    opacity: 0.7;
}

.form-submit-btn.loading::after {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 20px;
    height: 20px;
    border: 2px solid var(--pearl-white);
    border-top-color: transparent;
    border-radius: 50%;
    animation: btnSpinner 0.8s linear infinite;
}

@keyframes btnSpinner {
    to {
        transform: translate(-50%, -50%) rotate(360deg);
    }
}

/* Responsive adjustments for contact form */
@media (max-width: 768px) {
    .contact-section {
        padding: var(--space-xl) 0;
    }
    
    .contact-container {
        padding: 0 1.5rem;
    }
    
    .contact-header {
        flex-direction: column;
        gap: 1.5rem;
        padding: 1.5rem 0;
    }
    
    .contact-form {
        padding: 2rem 1.5rem;
        border-radius: 20px;
    }
    
    .form-row {
        grid-template-columns: 1fr;
        gap: 1.5rem;
    }
    
    .form-group {
        margin-bottom: 1.5rem;
    }
    
    .form-input,
    .form-textarea {
        font-size: 1rem;
        padding: 0.875rem 0;
    }
    
    .form-submit-btn {
        padding: 1rem 3rem;
        font-size: 0.875rem;
        width: 100%;
    }
    
    .form-message {
        font-size: 0.9375rem;
        padding: 0.875rem 1.5rem;
    }
}

@media (max-width: 480px) {
    .contact-section {
        padding: 3rem 0;
    }
    
    .contact-container {
        padding: 0 1rem;
    }
    
    .contact-header {
        padding: 1rem 0;
    }
    
    .contact-title .title-main {
        font-size: clamp(1.5rem, 8vw, 2rem);
        letter-spacing: 0.15em;
    }
    
    .contact-title .title-subtitle {
        font-size: clamp(0.875rem, 4vw, 1rem);
        letter-spacing: 0.15em;
    }
    
    .contact-form {
        padding: 1.5rem 1rem;
        border-radius: 16px;
    }
    
    .form-label {
        font-size: 0.75rem;
    }
    
    .form-input,
    .form-textarea {
        font-size: 0.9375rem;
    }
    
    .form-textarea {
        min-height: 100px;
    }
    
    .form-submit-btn {
        padding: 0.875rem 2.5rem;
        font-size: 0.8125rem;
        letter-spacing: 0.15em;
    }
    
    .btn-ornament {
        font-size: 0.875rem;
    }
}

/* Landscape mode optimization for contact form */
@media (max-height: 600px) and (orientation: landscape) {
    .contact-section {
        padding: 2rem 0;
    }
    
    .contact-header {
        padding: 1rem 0;
        margin-bottom: 2rem;
    }
    
    .form-group {
        margin-bottom: 1rem;
    }
    
    .form-textarea {
        min-height: 80px;
    }
    
    .form-submit-container {
        margin-top: 2rem;
    }
}
