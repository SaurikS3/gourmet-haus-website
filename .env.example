# Brevo SMTP Configuration
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=your_smtp_user@smtp-brevo.com
SMTP_PASS=your_smtp_password_here

# Email Configuration
RECIPIENT_EMAIL=your_recipient@example.com

# Optional: Brevo API Key (if using REST API instead)
BREVO_API_KEY=your_brevo_api_key_here

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual Brevo credentials
# 3. The .env file is ignored by git for security
